local main_state=
{
  waitForChest=0,
  getFromChest=1,
  insertDisk=2,
  waitCanLaunch=3,
  loadMissile=4,
  launch=5,
}
local s=main_state

state=s.waitForChest

local function main()

    while  true  do
        if(state==s.waitForChest)then
        
        elseif(state==s.getFromChest)then
        
        
        else
        
        end
    end

end